package start;

import javafx.fxml.FXML;
import javafx.scene.control.Label;

public class SplashScreen  {
    @FXML
    private Label progress;

   // @Override
    //public void start(Stage primaryStage) throws Exception{
        //new File("systemfiles").mkdir();
        //Parent root = FXMLLoader.load(getClass().getResource("splashScreen.fxml"));
        //preloaderStage.initStyle(StageStyle.UNDECORATED);
       // primaryStage.setScene(new Scene(root));
        //primaryStage.setTitle("HSRM");
        //Image applicationIcon = new Image(getClass().getResourceAsStream("/icons/icon.png"));
       // primaryStage.getIcons().add(applicationIcon);
        //primaryStage.initStyle(StageStyle.UNDECORATED);
        //primaryStage.setResizable(false);
       // Thread.sleep(1000);
        //primaryStage.show();
        //Thread.sleep(5000);
       // next stage
       // Stage stage = new Stage();
       // FXMLLoader loader = new FXMLLoader();
        //Pane root2 = (Pane)loader.load(getClass().getResource("../students/StudentsUpdated.fxml"));
        //Pane root2 = (Pane)loader.load(getClass().getResource("/F1/classlist/showClasslist2.fxml"));

        //StudentUpdated addStudentUpdated = (StudentUpdated) loader.getController();

        //Scene scene = new Scene(root2);
        //stage.setScene(scene);
       // stage.getIcons().add(applicationIcon);
       // stage.setTitle("ADD/EDIT STUDENT");
       // stage.setResizable(true);
        //Installation installation = new Installation();
        //installation.makeDirectories();
       // ConfigureApplication configureApplication = new ConfigureApplication();
        //configureApplication.CheckConnection();
        //configureApplication.CheckGrading(stage);
        //configureApplication.CheckComments();
       // configureApplication.CheckTeachers();
       // ServerStart serverStart = new ServerStart();
        //serverStart.StartServer();
        //serverStart.GetIp();
       // System.out.println("starting server");
       // AutoBackup autoBackup = new AutoBackup();
//        autoBackup.wait(5000);
        //System.out.println("started");
        //autoBackup.Backup();
       // DatabaseSync databaseSync = new DatabaseSync();
       // databaseSync.updateStudent();
       // databaseSync.addStudent(stage,primaryStage);

    //}


   // public static void main(String[] args) {
     //   launch(args);
   // }
}
